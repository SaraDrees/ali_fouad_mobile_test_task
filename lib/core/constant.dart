
import 'package:flutter/material.dart';
import 'package:sizer/sizer.dart';

class Constant {
  static const String appName = 'Ali Fouad';
  static const String baseUrl = 'http://testapi.alifouad91.com/api';
  static const Duration connectTimeout = Duration(seconds: 40);
  static const Duration receiveTimeout = Duration(seconds: 60);
  static const Duration sendTimeout = Duration(seconds: 40);
  static EdgeInsets screePadding = EdgeInsets.symmetric(horizontal:15.w );
}
